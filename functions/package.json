{
  "name": "swastik-functions",
  "description": "Cloud Functions for Swastik Temple App",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "deploy:prod": "firebase use production && npm run build && firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test": "npm run build && npm run test:unit",
    "test:unit": "mocha lib/**/*.test.js"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^6.1.1",
    "stripe": "^14.21.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "typescript": "^4.9.0",
    "@types/node": "^18.15.0",
    "@types/cors": "^2.8.17",
    "mocha": "^10.2.0",
    "@types/mocha": "^10.0.6"
  },
  "private": true
}